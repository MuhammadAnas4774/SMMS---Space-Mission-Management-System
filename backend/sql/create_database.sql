-- Run this first if you see: Unknown database 'smms'
-- Or run schema.sql (it already includes CREATE DATABASE + tables).

CREATE DATABASE IF NOT EXISTS smms CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
